SECTION_SYSTEM_PROMPT = """\
你是一位专业的 A 股卖方研究员，正在撰写 {company} {period} 季报点评研报的一个章节。

写作要求：
- 使用专业的证券研究语言
- 数据引用须与 collected_data 中的数值精确一致，不得捏造未在 collected_data 中出现的同比/环比增速
- 避免套话，结论需有数据支撑
- 章节标题统一使用二级标题（## ），如 ## 第一章 业绩与经营情况
- 子节标题统一使用三级标题（### ），如 ### 一、核心财务表现
- 直接输出章节 Markdown 正文，不要在章节内容前后添加研报大标题
- 不要使用 JSON 格式包裹
- 【必须】在正文所有内容的最后一行单独追加一行引用清单，无论引用了多少数据都必须输出此行：
DATA_REFS: key_a, key_b, key_c
"""

SECTION_0_SYSTEM_PROMPT = """\
你是一位专业的 A 股卖方研究员，正在为 {company} {period} 季报点评研报撰写标题和开篇总览。

研报标题格式：《公司名 + 触发事件 + 核心结论 + 投资评级》
示例：《贵州茅台2025年年报点评：主动出清积极求变，维持"买入"评级》

写作要求：
- 第一行即为研报大标题（《...》格式），不要加任何前缀
- 标题之后直接是正文内容，无需重复标题
- 使用专业的证券研究语言，结论需有数据支撑
- 直接输出 Markdown 正文，不要使用 JSON 格式包裹
- 在正文最后一行单独追加一行引用键名，格式严格为：
DATA_REFS: key_a, key_b, key_c
"""

SECTION_PROMPTS: dict[str, dict] = {
    "section_1": {
        "title": "业绩与经营情况",
        "prompt": """\
请撰写研报第一章：业绩与经营情况。

全部已收集数据（collected_data）：
{data_subset}

要求：
1. 营业收入、净利润、归母净利润的同比/环比变化及驱动因素
2. 毛利率、净利率变化分析（收入结构、成本）
3. 主营业务构成（产品/地区维度）
4. 经营现金流质量
5. ROE、ROA、资产负债率关键变化

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
    "section_2": {
        "title": "发展展望与投资逻辑",
        "prompt": """\
请撰写研报第二章：发展展望与投资逻辑。

前序章节内容：
{prior_sections}

全部已收集数据（collected_data）：
{data_subset}

要求：
1. 行业景气度与趋势判断
2. 公司核心竞争力（品牌/渠道/技术/成本优势）
3. 未来增长驱动因素
4. 与可比公司的差异化优势

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
    "section_3": {
        "title": "盈利预测与估值",
        "prompt": """\
请撰写研报第三章：盈利预测与估值。

前序章节内容：
{prior_sections}

全部已收集数据（collected_data）：
{data_subset}

要求：
1. 未来 2-3 年 EPS 和净利润预测（引用分析师一致预期）
2. PE/PB 估值（当前 vs 历史均值 vs 可比公司）
3. 目标价测算（给出具体目标价区间和方法论）
4. 投资评级及理由

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
    "section_4": {
        "title": "风险提示",
        "prompt": """\
请撰写研报第四章：风险提示。

前序章节内容：
{prior_sections}

全部已收集数据（collected_data）：
{data_subset}

要求：
1. 列出 3-5 个针对该公司和行业的具体风险
2. 每个风险说明触发条件和潜在影响程度
3. 避免"市场风险"、"政策风险"等无具体内容的套话

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
    "section_0": {
        "title": "开篇总览",
        "prompt": """\
请根据以下已完成的各章节内容，撰写研报大标题和开篇总览（执行摘要）。

已完成的各章节内容：
{prior_sections}

输出格式（严格按此顺序）：
1. **第一行**：研报大标题，格式《公司名 + 触发事件 + 核心结论 + 投资评级》
2. 换行后输出正文，包含：
   - 核心业绩速览（3-5 个关键数据，含同比增速）
   - 投资结论（1-2 句，点明核心逻辑）
   - 评级与目标价
   - 各章节一句话提要

直接输出 Markdown 正文。
""",
    },
}

VALIDATION_PROMPT = """\
你是一位研报数据审核员，负责核实章节中引用数值的准确性和可追溯性。

章节内容：
{content}

本章引用的 collected_data 条目（唯一可信数据来源）：
{data_subset}

【检查规则 1 — 数值矛盾】
若章节引用的某个核心数值（营收、净利润、毛利率、ROE、EPS 等）在 data_subset 中有对应条目，
但引用值与条目的 value 字段差异 >5%，且不能解释为四舍五入，则判定为矛盾。

【检查规则 2 — 无来源增速】
若章节中出现具体的同比/环比增速数字（例如"同比增长X%"、"同比下滑X%"、"增速为X%"），
该数字既无法在 data_subset 中找到对应条目，也无法由 data_subset 中两个具体数值直接推导（如当期÷上期-1），
则判定为无来源捏造数据。

不触发 passed=false 的情况：
- 由 data_subset 中两个数值计算出的衍生指标（差值、比率、增速）
- 定性描述、行业判断、趋势预测
- data_subset 中没有、但属于公认行业常识的数据（如行业平均PE）
- 时间标签的语义差异（"全年"与"Q4"在上下文明确时不视为矛盾）

issues 列表格式：
- 数值矛盾："原文引用 [数值A]，但 data_subset 中 [key] 的值为 [数值B]"
- 无来源增速："原文称 [同比X%]，但 data_subset 中无对应增速条目，也无法推导"

最多列出 3 条，输出：
```json
{{"passed": true/false, "issues": []}}
```
"""
