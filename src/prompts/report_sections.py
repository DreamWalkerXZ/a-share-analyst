SECTION_SYSTEM_PROMPT = """\
你是一位专业的 A 股卖方研究员，正在撰写 {company} {period} 季报点评研报的一个章节。

写作要求：
- 使用专业的证券研究语言
- 数据引用须与 collected_data 中的数值精确一致，不得捏造未在 collected_data 中出现的同比/环比增速
- 避免套话，结论需有数据支撑
- 章节标题统一使用二级标题（## ），如 ## 第一章 业绩与经营情况
- 子节标题统一使用三级标题（### ），如 ### 一、核心财务表现
- 直接输出章节 Markdown 正文，不要在章节内容前后添加研报大标题
- 不要使用 JSON 格式包裹
- 【必须】在正文所有内容的最后一行单独追加一行引用清单，无论引用了多少数据都必须输出此行：
DATA_REFS: key_a, key_b, key_c

DATA_REFS 规则：
- 每个 key 必须与 collected_data 中的实际 key 完全一致（原样复制，不得重命名、缩写或自创）
- 例如：若 collected_data 中有 "{company}_2025年_毛利率_酒类"，则 DATA_REFS 必须用此精确名称，不得改成别的
- 若不确定某指标对应哪个 key，请直接从 collected_data 的 JSON 中查找
"""

SECTION_0_SYSTEM_PROMPT = """\
你是一位专业的 A 股卖方研究员，正在为 {company} {period} 季报点评研报撰写标题和开篇总览。

研报标题格式：《公司名 + 触发事件 + 核心结论 + 投资评级》
示例：《XX公司2025年年报点评：核心结论，维持"买入"评级》

写作要求：
- 第一行即为研报大标题（《...》格式），不要加任何前缀
- 标题之后直接是正文内容，无需重复标题
- 使用专业的证券研究语言，结论需有数据支撑
- 直接输出 Markdown 正文，不要使用 JSON 格式包裹
- 在正文最后一行单独追加一行引用键名，格式严格为：
DATA_REFS: key_a, key_b, key_c

DATA_REFS 规则：每个 key 必须与 collected_data 中的实际 key 完全一致，不得重命名或自创。
  若不确定对应哪个 key，请直接从 collected_data 的 JSON 中查找。
"""

SECTION_PROMPTS: dict[str, dict] = {
    "section_1": {
        "title": "业绩与经营情况",
        "prompt": """\
请撰写研报第一章：业绩与经营情况。

全部已收集数据（格式：key | 字段名: 值 单位 (报告期)；DATA_REFS 中必须使用左侧的 key 原名）：
{data_subset}

要求：
1. 营业收入、净利润、归母净利润的同比/环比变化及驱动因素
2. 毛利率、净利率变化分析（收入结构、成本）
3. 主营业务构成（产品/地区维度）
4. 经营现金流质量
5. ROE、ROA、资产负债率关键变化

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
    "section_2": {
        "title": "发展展望与投资逻辑",
        "prompt": """\
请撰写研报第二章：发展展望与投资逻辑。

前序章节内容：
{prior_sections}

全部已收集数据（格式：key | 字段名: 值 单位 (报告期)；DATA_REFS 中必须使用左侧的 key 原名）：
{data_subset}

要求：
1. 行业景气度与趋势判断
2. 公司核心竞争力（品牌/渠道/技术/成本优势）
3. 未来增长驱动因素
4. 与可比公司的差异化优势

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
    "section_3": {
        "title": "盈利预测与估值",
        "prompt": """\
请撰写研报第三章：盈利预测与估值。

前序章节内容：
{prior_sections}

全部已收集数据（格式：key | 字段名: 值 单位 (报告期)；DATA_REFS 中必须使用左侧的 key 原名）：
{data_subset}

要求：
1. 未来 2-3 年 EPS 和净利润预测（引用分析师一致预期）
2. PE/PB 估值（当前 vs 历史均值 vs 可比公司）
3. 目标价测算（给出具体目标价区间和方法论）
4. 投资评级及理由

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
    "section_4": {
        "title": "风险提示",
        "prompt": """\
请撰写研报第四章：风险提示。

前序章节内容：
{prior_sections}

全部已收集数据（格式：key | 字段名: 值 单位 (报告期)；DATA_REFS 中必须使用左侧的 key 原名）：
{data_subset}

要求：
1. 列出 3-5 个针对该公司和行业的具体风险
2. 每个风险说明触发条件和潜在影响程度
3. 避免"市场风险"、"政策风险"等无具体内容的套话

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
    "section_0": {
        "title": "开篇总览",
        "prompt": """\
请根据以下已完成的各章节内容，撰写研报大标题和开篇总览（执行摘要）。

已完成的各章节内容：
{prior_sections}

全部已收集数据（格式：key | 字段名: 值 单位 (报告期)；DATA_REFS 中必须使用左侧的 key 原名）：
{data_subset}

输出格式（严格按此顺序）：
1. **第一行**：研报大标题，格式《公司名 + 触发事件 + 核心结论 + 投资评级》
2. 换行后输出正文，包含：
   - 核心业绩速览（3-5 个关键数据，含同比增速）
   - 投资结论（1-2 句，点明核心逻辑）
   - 评级与目标价
   - 各章节一句话提要

直接输出章节 Markdown 正文，不要使用 JSON 格式。
""",
    },
}

VALIDATION_PROMPT = """\
你是一位研报数据审核员，负责核实章节中引用数值的准确性和可追溯性。

章节内容：
{content}

全部可用数据（格式：key | 字段名: 值 单位 (报告期) [notes]）：
{all_data}

查找规则：验证章节中的引用数值时，在上方列表中搜索语义相近的 key；
若上方列表中完全找不到对应数据，才按"无来源"处理。

【检查规则 1 — 数值矛盾】
若章节引用的某个核心数值在全部可用数据中找到对应条目，
但引用值与条目的 value 字段存在实质性偏差，则判定为矛盾。

实质性偏差的定义：
- 绝对值类（亿元、元/股等）：|引用值 - 实际值| / |实际值| > 0.05（即相对偏差超过 5%）
  例：引用 1688.38 亿元，实际 1688.381 亿元 → 相对差 0.0006% → 不矛盾（远低于 5%）
  例：引用 200 亿元，实际 411 亿元 → 相对差 51% → 矛盾
- 百分比类（增速、毛利率、ROE 等，单位 %）：|引用值 - 实际值| > 0.5 个百分点
  例：引用 -19.35%，实际 -19.3472% → 差 0.003pp → 不矛盾
  例：引用 91.23%，实际 90.82% → 差 0.41pp → 不矛盾（<0.5pp）

绝对不能判为矛盾的情况：
- 仅因显示精度/四舍五入导致的微小差异（如 411.50 vs 411.5028，相对差 0.0007%）
- 单位换算展示差异（亿元/元舍入）
- key 名称与原文表述不同（如 "归母净利润同比增速" vs "PARENTNETPROFITTZ"）但数值一致或差异在阈值内
- 时间标签格式不同（如 "2025H1" vs "2025年上半年"）但数值一致或差异在阈值内

【检查规则 2 — 无来源数据】
若章节出现具体数值，且满足以下全部条件，才判定为无来源：
1. 在全部可用数据中均找不到对应条目
2. 无法由可用数据中任意两个数值直接推导

常见可直接推导的衍生指标（有原材料即可推导，不算无来源）：
- 资产负债率 = 总负债 ÷ 总资产
- 毛利率 = (营业收入 - 营业成本) ÷ 营业收入
- 净利率 = 净利润 ÷ 营业收入
- 同比增速 = 当期值 ÷ 同期值 - 1
- 环比增速 = 当期值 ÷ 上期值 - 1
- 营收占比 = 某业务收入 ÷ 总营业收入
- 每股指标 = 总额 ÷ 总股本
- 倍数/比率 = 任意两个同口径数值之比

不触发 fail 的情况：
- 可由可用数据推导出的衍生指标
- 定性描述、行业判断、趋势预测
- 公认行业常识数据
- 时间标签的语义差异
- 无来源的预测性观点

---

逐条检查章节中出现的每个数值引用，仅输出以下 JSON（不要输出任何其他文字）：

```json
{{{{
  "refs": [
    {{{{
      "ref_value": "章节中引用的数值原文（含单位）",
      "ref_key": "collected_data 中匹配到的 key，未匹配则为 null",
      "actual_value": "匹配到的实际值（含单位），未匹配则为 null",
      "deviation": "偏差计算结果，如 '0%'、'0.00pp'，未匹配则为 null",
      "result": "pass"
    }}}},
    {{{{
      "ref_value": "85.15%",
      "ref_key": "某公司_2025年_主营收入占比",
      "actual_value": "86.81 %",
      "deviation": "1.91%",
      "result": "fail"
    }}}},
    {{{{
      "ref_value": "2300元/瓶",
      "ref_key": null,
      "actual_value": null,
      "deviation": null,
      "result": "no_source"
    }}}}
  ],
  "passed": true,
  "issues": []
}}}}
```

result 取值：
- pass：数值与数据一致或差异在阈值内
- fail：存在实质性偏差（仅 result=fail 的条目写入 issues）
- derivable：无直接匹配条目，但可由已有数据推导（不写入 issues）
- no_source：无匹配且无法推导（不写入 issues）

issues 格式（最多 3 条，全部通过时为空列表 []）：
- "原文引用 [数值A]，但可用数据中 [key] 的值为 [数值B]，相对差 X%"
"""
